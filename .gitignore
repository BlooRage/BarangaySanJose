# Runtime uploads and generated documents must stay out of Git. On the server,
# deploy_hostinger.sh links this path to persistent storage outside the checkout.
UnifiedFileAttachment/

# Existing local ignore retained.
JS-Script-Files/Resident-End/Clearances/electricalPermitScript.js

# Local runtime configuration and secrets must never be committed.
config.runtime.local.php
config.runtime.php
PhpFiles/General/runtime.local.php
PhpFiles/General/runtime.php
.env
.env.*
hi.php

# Disposable maintenance routing cache; durable state lives in the database.
.maintenance-mode.flag
.maintenance-settings.json
